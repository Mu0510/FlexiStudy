feat: Integrate jsdiff and fix useEffect scope in useChat.ts

This commit addresses the integration of the `jsdiff` library for accurate diff rendering and resolves a persistent syntax error in `useChat.ts` related to the `useEffect` hook's scope.

- `webnew/hooks/useChat.ts`:
    - Imported `Diff` from `jsdiff` and updated `generateContextualDiffHtml` to use `Diff.structuredPatch` for precise diff generation, mirroring `chat.js`'s functionality.
    - Corrected the scope of `ws.current.onclose` and `ws.current.onerror` event handlers by moving them inside the `useEffect` hook, resolving the "Expected ';', '}' or <eof>" syntax error.
- `webnew/components/new-chat-panel.tsx`:
    - Added `marked` import to ensure proper rendering of markdown content.
- `webnew/package.json` & `webnew/pnpm-lock.yaml`:
    - Added `diff` and `jsdiff` as dependencies to support diff rendering.