feat(search): 検索結果表示の改善と詳細情報の追加

- `manage_log.py` で学習ログの `content` と `event_type` を検索結果に含めるように変更。
- `webnew/components/study-records.tsx` で検索結果のタイトル表示ロジックを改善し、`subject` と `tags` の表示を追加。
- スニペット表示を `memo`, `impression`, `session_summary` に限定。